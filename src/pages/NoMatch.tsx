const NoMatch = () => {
    return (
        <div className="h-full min-h-[50vh] w-full flex items-center justify-center text-blueGray">
            This page {`doesn't`} exist
        </div>
    );
};

export default NoMatch;
